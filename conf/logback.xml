<!-- https://www.playframework.com/documentation/latest/SettingsLogger -->
<configuration>

  <conversionRule conversionWord="coloredLevel" converterClass="play.api.libs.logback.ColoredLevel" />

  <appender name="FILE" class="ch.qos.logback.core.FileAppender">
    <file>${application.home:-.}/logs/application.log</file>
    <encoder>
      <pattern>%date [%level] from %logger in %thread - %message%n%xException</pattern>
    </encoder>
  </appender>

  <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>%coloredLevel %logger{15} - %message%n%xException{10}</pattern>
    </encoder>
  </appender>

  <appender name="ASYNCFILE" class="ch.qos.logback.classic.AsyncAppender">
    <appender-ref ref="FILE" />
  </appender>

  <appender name="ASYNCSTDOUT" class="ch.qos.logback.classic.AsyncAppender">
    <appender-ref ref="STDOUT" />
  </appender>

  <!-- Configurar logs generales -->
  <logger name="play" level="INFO" />
  <logger name="application" level="DEBUG" />

  <!-- Logs detallados de Hibernate -->
  <logger name="org.hibernate" level="DEBUG" />
  <logger name="org.hibernate.cfg" level="DEBUG" />
  <logger name="org.hibernate.tool.hbm2ddl" level="DEBUG" />
  <logger name="org.hibernate.SQL" level="DEBUG" />
  <logger name="org.hibernate.type.descriptor.sql" level="TRACE" />

  <!-- Logs detallados de JPA -->
  <logger name="javax.persistence" level="DEBUG" />

  <!-- Logs de JAXB (para ver si está fallando la carga de clases) -->
  <logger name="javax.xml.bind" level="DEBUG" />
  <logger name="com.sun.xml.bind" level="DEBUG" />
  <logger name="org.glassfish.jaxb" level="DEBUG" />

  <!-- Mostrar problemas de carga de clases -->
  <logger name="org.springframework.beans" level="DEBUG" />
  <logger name="org.springframework.context" level="DEBUG" />
  <logger name="org.springframework.core" level="DEBUG" />

  <!-- Off estos logs que no son necesarios -->
  <logger name="com.avaje.ebean.config.PropertyMapLoader" level="OFF" />
  <logger name="com.avaje.ebeaninternal.server.core.XmlConfigLoader" level="OFF" />
  <logger name="com.avaje.ebeaninternal.server.lib.BackgroundThread" level="OFF" />
  <logger name="com.gargoylesoftware.htmlunit.javascript" level="OFF" />

  <root level="WARN">
    <appender-ref ref="ASYNCFILE" />
    <appender-ref ref="ASYNCSTDOUT" />
  </root>

</configuration>
